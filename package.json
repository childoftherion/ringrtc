{
  "name": "@signalapp/ringrtc",
  "version": "2.60.1",
  "private": true,
  "description": "Signal Messenger voice and video calling library - Root package pointing to src/node",
  "repository": {
    "type": "git",
    "url": "https://github.com/childoftherion/ringrtc.git"
  },
  "main": "./src/node/dist/index.js",
  "types": "./src/node/dist/index.d.ts",
  "files": [
    "src/node/**/*"
  ],
  "scripts": {
    "install": "cd src/node && npm_package_json=\"$(pwd)/package.json\" npm_package_version=$(grep -o '\"version\": \"[^\"]*\"' package.json | cut -d'\"' -f4) node scripts/fetch-prebuild.js"
  }
}

